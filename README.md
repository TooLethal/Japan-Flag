# 🇯🇵 CSS Flag of Japan

A simple **HTML + CSS project** that recreates the **national flag of Japan** using styled div elements. The design features a white rectangular background with a centered red circle, symbolizing the sun.

## ✨ Features
- **Accurate Colors**  
  - White → `#FFFFFF`  
  - Red → `#BC022C`  

- **Centered Circle**  
  - A perfectly round red circle (`.circle`) positioned in the middle of the flag using `transform: translate(-50%, -50%)`.  

- **Responsive Centering**  
  - Flexbox ensures the flag is centered both vertically and horizontally on the page.  

- **Customizable Variables**  
  - Colors and flag height defined in `:root` for easy adjustments.  

- **Minimal Structure**  
  - Only two elements: `.flag-japan` (background) and `.circle` (sun).  

## 🛠️ Tech Stack
- **HTML5**  
- **CSS3 (Flexbox + Variables)**  

## 🚀 Use Cases
- Practicing CSS layout and positioning  
- Demonstrating how to build flags with pure CSS  
- Educational projects for learning Flexbox and absolute positioning  
- Creative UI elements for themed websites  

## 📂 Project Structure
```
index.html   # Main HTML structure
style.css    # Flag styling and layout
```
